! Config example for Nexus 3064

hostname {{ hostname }}

md5_location tftp:/{{ tftp_server}}/cisco/{{ hostname }}.md5
file_location tftp:/{{ tftp_server}}/cisco/{{ hostname }}.cfg

int mgmt 0
 ip address {{ vl1ip }}

Rest of the config

